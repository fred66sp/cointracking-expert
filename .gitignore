# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
pip-wheel-metadata/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# Virtual environments
venv/
ENV/
env/
.venv

# IDE
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# Testing
.pytest_cache/
.coverage
htmlcov/
.tox/
.hypothesis/

# Environment / secretos — NUNCA subir credenciales
.env
.env.*
.env_*
*.env

# OS
Thumbs.db
.DS_Store

# Project specific
reports/output/
*.db
*.sqlite
*.sqlite3

# Datos reales de usuario (privacidad) — NUNCA subir exportaciones reales
# Contienen historial financiero personal. Usar fixtures anonimizados en tests/.
*.csv
!tests/fixtures/**/*.csv

# Carpeta de entrada del usuario: aquí deja sus CSV u otras fuentes que le
# pidamos (Excel, JSON, PDF, capturas...). Su contenido es DATO REAL y NUNCA
# se versiona. Solo se versiona el README de instrucciones.
USER_INPUT/*
!USER_INPUT/README.md

# Caché de datos de CoinTracking (snapshots MCP/API) — datos reales, NUNCA subir (ADR-010)
.cache/

# Servidor MCP de CoinTracking: dependencia externa de terceros (MIT, repo
# aparte). Se instala/clona localmente; no se versiona aquí. La integración
# (configuración + documentación) sí es nuestra: .mcp.json y
# knowledge/cointracking/MCP_API.md.
cointracking-mcp-main/
